/**
 * San Diego Family Travel Intelligence - ESSENTIAL TASKS ONLY
 * Focus: Critical logistics and weather considerations
 */

export function generateSanDiegoTasks(familyProfile: any, daysUntilTrip: number) {
  const tasks = [];
  const kids = familyProfile.kids || [];
  const tripPurpose = familyProfile.tripPurpose;
  
  // Generally San Diego is very easy-going, no critical bookings required
  // Main considerations are weather (always good) and choosing between attractions
  
  return tasks;
}